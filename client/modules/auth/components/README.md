# Auth components

This folder contains UI components related to authentication (login, signup, password reset). It is intentionally empty right now.

Add components here and remove this file when the folder contains real code.