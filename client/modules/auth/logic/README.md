# Auth logic

Business logic and helper utilities for authentication (e.g., form validation, API clients). This folder is currently empty.

Add logic files here and remove this README when the folder contains real code.